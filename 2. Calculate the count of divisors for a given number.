#include<stdio.h>
int main()
{
    int n, div;
    printf("Enter the number :");
    scanf("%d",&n);
    printf("All the divisors of %d are : ",n);
    for(div=1;div<=n;div++)
    {
        if(n%div==0)
        {
            printf ("\n%d",div);
        }
    }
    return 0;
}
